import { DpArrowNavigationDirective } from './dp-arrow-navigation.directive';

describe('DpArrowNavigationDirective', () => {
   it('should create an instance', () => {
      const directive = new DpArrowNavigationDirective(null);
      expect(directive).toBeTruthy();
   });
});
