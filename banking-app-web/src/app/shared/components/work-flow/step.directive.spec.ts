import { StepDirective } from './step.directive';

describe('StepDirective', () => {
   it('should create an instance', () => {
      const directive = new StepDirective(null);
      expect(directive).toBeTruthy();
   });
});
