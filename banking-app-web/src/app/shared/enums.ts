export enum AlertActionType {
    None = 1,
    Close = 2,
    TryAgain = 3,
    ForgotDetails = 4,
    Help = 5,
    ResendDetails = 6
}

export enum AlertMessageType {
    Error = 1,
    Lock = 2,
    Success = 3
}

