export enum NotificationTypes {
   Error = 1,
   Success = 2,
   Warning = 3,
   None = 4
}

export enum SysErrorInstanceType {
   Body = 1,
   Modal = 2
}

export enum PaymentType {
   account = 1,
   mobile = 2,
   creditCard = 3,
   foreignBank = 4
}

export enum ISettlementNotificationTypes {
   QuoteGenerateError = 1,
   TechnicalError = 2,
   ClosureProcess = 3,
   None = 4
}
